--- a/src/main.rs
+++ b/src/main.rs
@@ -530,7 +530,7 @@ fn show(state: &mut AppState, qh: &QueueHandle<AppState>) -> bool {
     let layer_surface = layer_shell.get_layer_surface(
         &wl_surf,
         None,
-        zwlr_layer_shell_v1::Layer::Top,
+        zwlr_layer_shell_v1::Layer::Overlay,
         "hyprexpose".to_owned(),
         qh,
         (),

--- a/src/ipc.rs
+++ b/src/ipc.rs
@@ -200,7 +200,7 @@ fn dispatch(cmd: &str) {
 }

 pub fn switch_workspace(id: i32) {
-    dispatch(&format!("hl.dsp.focus({{ workspace = \"{id}\" }})"));
+    dispatch(&format!("workspace {id}"));
 }

 pub fn move_window_to_workspace(window_address: u64, workspace_id: i32) {
